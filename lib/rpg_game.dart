import 'dart:io';
import 'dart:math';
import 'package:rpg_game/vo/character.dart';
import 'package:rpg_game/vo/monster.dart';

class RpgGame {
  late Character character;
  List<Monster> monster = [];

  RpgGame(this.character, this.monster);

  void startGame() {
    while (character.health > 0 && )
  }
}
